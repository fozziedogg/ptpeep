# Xcode build output (was custom-located at ./build — must NOT be tracked;
# XCBuildData embeds machine-specific absolute paths that break other machines)
build/
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# direnv (local env)
.envrc
.direnv/

# App runtime log (may get dropped into Tests/ for debugging)
ptpeep.log
